/**
 * Configuración base de la aplicación.
 * Centraliza la URL del backend para facilitar cambios por entorno.
 */
export const APP_CONFIG = {
  /** URL base del API (ej: http://localhost:3000) */
  baseUrl: 'http://localhost:3000',
} as const;
